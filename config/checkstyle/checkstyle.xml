<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
    "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
    "https://checkstyle.org/dtds/configuration_1_3.dtd">

<!--
  Practical, not pedantic. This config is intentionally moderate for educational examples.
  You can tighten rules later once the repo stabilizes.
-->

<module name="Checker">
  <module name="SuppressionFilter">
    <property name="file" value="config/checkstyle/suppressions.xml"/>
  </module>

  <module name="TreeWalker">

    <!-- Basic correctness / readability -->
    <module name="UnusedImports"/>
    <module name="AvoidStarImport"/>
    <module name="OneTopLevelClass"/>
    <module name="NeedBraces"/>
    <module name="EmptyBlock">
      <property name="option" value="TEXT"/>
    </module>

    <!-- Naming (kept light) -->
    <module name="TypeName"/>
    <module name="MethodName"/>
    <module name="ParameterName"/>
    <module name="LocalVariableName"/>

    <!-- Complexity (light guardrails) -->
    <module name="CyclomaticComplexity">
      <property name="max" value="12"/>
    </module>

    <!-- Javadoc: optional, not enforced in examples (commented out) -->
    <!--
    <module name="JavadocMethod">
      <property name="scope" value="public"/>
      <property name="allowMissingParamTags" value="true"/>
      <property name="allowMissingReturnTag" value="true"/>
      <property name="allowMissingThrowsTags" value="true"/>
    </module>
    -->
  </module>
</module>
